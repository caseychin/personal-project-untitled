# Ingestion-script config. Separate from .env on purpose — app code and
# tests never see the service-role key (see CLAUDE.md and .env.example).
# Copy to .env.ingest (git-ignored) and fill in real values pulled fresh
# from the Supabase dashboard. Dev project only for now.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
